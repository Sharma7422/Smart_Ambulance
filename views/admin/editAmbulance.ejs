<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Ambulance</title>
    <!-- <link rel="stylesheet" href="/css/styles.css"> -->
    <style>
        /* Form Container */
        .form-container {
            background: #f9f9f9;
            padding: 40px;
            border-radius: 16px;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 600px;
            margin: 50px auto;
            transition: transform 0.3s ease;
        }

        .form-container:hover {
            transform: translateY(-5px);
        }

        .form-container h2 {
            margin-bottom: 20px;
            color: #000;
            font-weight: bold;
            font-size: 28px;
            text-align: center;
        }

        /* Input and button styles */
        .form-control{
            border-radius: 8px;
            padding: 12px;
            font-size: 16px;
            margin-bottom: 15px;
            width: 100%;
            background-color: #f2f2f2;
            color: #333;
            border: 1px solid #ddd;
            transition: border-color 0.3s ease;
        }

        .form-control:focus {
            border-color: #5d3ebc;
            outline: none;
            box-shadow: 0 0 10px rgba(93, 62, 188, 0.3);
        }

        /* Button Styling */
        .btn-primary {
            background: linear-gradient(135deg, #4CAF50, #2E7D32);
            color: white;
            border: none;
            padding: 14px;
            border-radius: 8px;
            font-size: 18px;
            width: 100%;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        .btn-primary:hover {
            background: linear-gradient(135deg, #2E7D32, #4CAF50);
        }

        /* Labels */
        label {
            font-weight: 600;
            color: #333;
            font-size: 15px;
            margin-bottom: 6px;
            display: block;
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .form-container {
                padding: 30px;
            }

            .btn-primary {
                font-size: 16px;
            }

            .form-container h2 {
                font-size: 22px;
            }
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h2>Edit Ambulance Details</h2>
    
    <form action="/admin/edit-ambulance/<%= ambulance._id %>" method="POST">
        <div class="mb-3">
        <label>Driver Name:</label>
        <input type="text" class="form-control" name="driverName" value="<%= ambulance.driverName %>" required>
       </div>

       <div class="mb-3">
        <label>Vehicle Number:</label>
        <input type="text" class="form-control" name="vehicleNumber" value="<%= ambulance.vehicleNumber %>" required>
        </div>

        <div class="mb-3">
        <label>Driver Contact:</label>
        <input type="text" class="form-control" name="driverNumber" value="<%= ambulance.driverNumber %>" required>
        </div>

        <div class="mb-3">
        <label>Hospital:</label>
        <input type="text" class="form-control" name="driverHospital" value="<%= ambulance.driverHospital %>" required>
        </div>

        <div class="mb-3">
        <label>Vehicle Type:</label>
        <input type="text" class="form-control" name="vehicleType" value="<%= ambulance.vehicleType %>" required>
        </div>

        <button type="submit" class="btn-primary">Update</button><br><br>
        <a href="/admin/ambulances"><button type="button" class="btn-primary">Cancel</button></a>
    </form>
</div>
</body>
</html>
